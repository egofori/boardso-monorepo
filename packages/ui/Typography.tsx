"use client"

import { Typography, type TypographyProps } from "@material-tailwind/react"
import React from "react"

// export default function UITypography({ children, className, ...rest } : TypographyProps) {
//   return (
//     <Typography className={`text-[#0f172a] ${className}`} {...rest}>{children}</Typography>
//   )
// }

export { Typography as UITypography }
